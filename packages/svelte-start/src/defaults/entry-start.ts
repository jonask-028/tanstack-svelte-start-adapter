/**
 * Default Start Entry Point
 *
 * This file is loaded by the core plugin to configure `createStart()`.
 * Users can create `src/start.ts` in their project to customize
 * serialization adapters, default SSR mode, and middleware.
 *
 * The default export is `undefined`, which means no custom configuration.
 */

export const startInstance = undefined;
